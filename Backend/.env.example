# Backend environment variables

# Backend server
PORT=5600
# Comma-separated allowed origins for production; localhost dev ports are allowed automatically.
FRONTEND_ORIGIN=http://localhost:5173,http://localhost:5174

# MongoDB
# Use your MongoDB Atlas connection string OR local MongoDB.
# Example Atlas: mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/<db>?retryWrites=true&w=majority
MONGODB_URI=enter URL

# Auth
JWT_SECRET=change_me

# Google OAuth (for "Sign in with Google")
# Create an OAuth Client ID in Google Cloud Console and paste it here.
GOOGLE_CLIENT_ID=your_google_oauth_client_id

# Optional: only needed if you implement OAuth code-flow on backend.
# Do NOT commit real secrets.
GOOGLE_CLIENT_SECRET=your_google_oauth_client_secret

# AI (Gemini)
# Get your key from Google AI Studio
GEMINI_API_KEY=your_key_here
# Optional
GEMINI_MODEL=gemini-flash-latest
