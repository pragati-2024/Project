# Vite env vars must start with VITE_

# Google OAuth (for "Sign in with Google")
VITE_GOOGLE_CLIENT_ID=your_google_oauth_client_id

# If you use the Vite proxy, you can leave this unset and calls will go to '/api/users'
# If you want to bypass proxy (or in production), set full backend URL:
# VITE_API_BASE_URL=http://localhost:5600/api/users

# Example for Gemini key (if used in the app):
# VITE_GEMINI_API_KEY=your_key_here
